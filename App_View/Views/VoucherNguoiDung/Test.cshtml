@using System.Globalization;
@using static App_Data.Repositories.TrangThai;

@{
    Layout = "/Views/Shared/_LayoutUser.cshtml";
    //Layout = "/Views/Shared/_LayoutLogin.cshtml";
    //Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="col-lg-9 col-md-12">
    <div class="dash__box dash__box--shadow dash__box--radius dash__box--bg-white u-s-m-b-30">

        <div class="dash__pad-2">
            <div>
                <h1 class="dash__h1 u-s-m-b-14" style="display: inline-block;">My Voucher</h1>
                <a style="color:#FF4500;margin-left:32%;display: inline-block;">Tìm thêm voucher</a> |
                <a style="color:#FF4500" style="display: inline-block;">Xem lịch sử voucher</a> |
                <a style="color:#FF4500" style="display: inline-block;">Tìm hiểu</a>
            </div>
            <span class="dash__text u-s-m-b-30">Bạn sẽ thấy tất cả voucher đã lưu của bạn ở đây.</span>
            <!--Lưu mã voucher-->
            <div class="row">
                <div class="col-lg-10 u-s-m-b-30" style="text-align:center">
                    <input b-xzovs66yy5="" class="input-text input-text--border-radius input-text--style-1" type="text" id="main-search" placeholder="Nhập mã voucher">
                </div>
                <div class="col-lg-2" style="text-align:center">
                    <button class="btn btn--e-brand newsletter__btn" style="margin-top: -15px; border-radius: 40px;margin-right:50px" type="submit">
                        Lưu
                    </button>
                </div>
            </div>
            <!--Show mã voucher-->
            <form class="m-order u-s-m-b-30">
                <div class="m-order__select-wrapper">
                    <label class="u-s-m-r-8" for="my-order-sort">Lọc voucher theo:</label><select class="select-box select-box--primary-style" id="my-order-sort">
                        <option label="Vui lòng chọn"></option>
                        <option value="@((int)TrangThaiLoaiKhuyenMai.TienMat)">Giảm giá tiền mặt</option>
                        <option value="@((int)TrangThaiLoaiKhuyenMai.PhanTram)">Giảm giá theo %</option>
                        <option value="@((int)TrangThaiLoaiKhuyenMai.Freeship)">Miễn phí ship</option>
                    </select>
                </div>
            </form>
            <div class="row">
            </div>
            <div class="m-order__list">
                <div class="col-lg-9 col-md-12">
                    <div class="dash__box dash__box--shadow dash__box--radius dash__box--bg-white u-s-m-b-30">
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex justify-content-between p-b-20 border-bottom">
                                    <div class="media align-items-center">
                                        <div class="avatar avatar-blue avatar-icon" style="height: 55px; width: 55px;">
                                            <i class="anticon anticon-gift" font-size-25" style="line-height: 55px"></i>
                                        </div>
                                        <div class="m-l-15">
                                            <h3 class="font-weight-bold font-size-30 m-b-0">
                                                Giảm 10%
                                                @*                          @if (Model.LoaiHinhUuDai == 1)
                                                {
                                                <span>Giảm @Model.MucUuDai %</span>
                                                }
                                                else
                                                {
                                                double? mucUuDai = Model.MucUuDai;
                                                @string.Format("{0:N0}đ", Model.MucUuDai)
                                                // Định dạng giá trị theo định dạng tiền Việt Nam
                                                } *@
                                            </h3>
                                            @*    @functions {
                                            // Hàm trợ giúp để định dạng số nguyên thành tiền tệ Việt Nam
                                            public string FormatCurrency(int? value)
                                            {
                                            return string.Format(new CultureInfo("vi-VN"), "{0:C0}", value);
                                            }
                                            public string FormatDate(DateTime? value)
                                            {
                                            if (value.HasValue)
                                            {
                                            return value.Value.ToString("dd.MM", new System.Globalization.CultureInfo("vi-VN"));
                                            }
                                            return string.Empty; // hoặc bạn có thể trả về một giá trị mặc định khác nếu cần
                                            }

                                            } *@
                                            <h5 class="m-b-0">
                                                <i class="anticon anticon-clock-circle"></i>
                                                @*       Có hiệu lực từ @FormatDate(Model.NgayBatDau) *@
                                                Có hiệu lực từ 14-02-03
                                            </h5>
                                        </div>
                                    </div>
                                </div>
                                <ul class="list-unstyled m-v-30">
                                    <li class="m-b-20">
                                        <h4 class="m-b-0">  Giảm cho đơn từ 100k  @* @FormatCurrency(Model.DieuKien) *@</h4>
                                        <div class="d-flex justify-content-between">
                                            <p style="color:red">
                                                <i class="anticon anticon-clock-circle"></i>
                                                Hạn sử dụng mã từ @* @FormatDate(Model.NgayBatDau) *@ 14-02 - @* @FormatDate(Model.NgayKetThuc) *@ 20-10
                                            </p>
                                        </div>
                                    </li>
                                    <li class="m-b-20">
                                    </li>
                                    <li class="m-b-20">
                                        <div class="d-flex justify-content-between">
                                            <span class="text-dark font-weight-semibold">    Xem chi tiết</span>
                                        </div>
                                    </li>
                                    <li class="m-b-20">
                                        <div class="d-flex justify-content-between">
                                            <p>
                                                Xem chi tiết
                                                Mã giảm theo tiền  @* @if (Model.LoaiHinhUuDai == 1)
                                                {
                                                // Chuyển đổi giá trị thành phần trăm và định dạng chuỗi
                                                string mucUuDaiPhanTram = string.Format("{0:P}", Model.MucUuDai);
                                                }
                                                else
                                                {
                                                // Định dạng giá trị theo định dạng tiền Việt Nam
                                                @string.Format("{0:N0}đ", Model.MucUuDai)

                                                } *@ cho đơn hàng từ @* @FormatCurrency(Model.DieuKien) *@ 40K sử dụng trên trang web.<br />
                                                Số lượng mã giảm giá còn hạn 10 @* @Model.SoLuong. *@ Hạn sử dụng đến @* @Model.NgayKetThuc *@ 20-10
                                            </p>
                                    </li>
                                </ul>
                                <div class="text-center">
                                    <button class="btn btn-primary">Copy mã</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="m-order__get">
                    <div class="manage-o__header u-s-m-b-30">
                        <div class="dash-l-r">
                            <div>
                                <div class="manage-o__text-2 u-c-secondary">Tên voucher ghi ở đây</div>
                                <div class="manage-o__text u-c-silver">Hạn sử dụng: 14-02-03</div>
                            </div>
                            <div>
                                <div class="dash__link dash__link--brand">

                                    <a href="dash-manage-order.html">Chi tiết</a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="manage-o__description">
                        <div class="description__container">
                            <div class="description__img-wrap">

                                <img class="u-img-fluid" src="/images/VoucherIMG/MyLeader.png" alt="">
                            </div>
                            <div class="description-title">
                                <p> Giảm: 100K</p>
                                <p width:40px>Đơn tối thiểu: 100K</p>
                            </div>

                        </div>
                        <div class="description__info-wrap">
                            <div>

                                <span class="manage-o__badge badge--delivered">Copy mã</span>
                            </div>
                        </div>
                    </div>
                </div>
                
               
            </div>
        </div>
    </div>
</div>
</div>


<div class="col-lg-9 col-md-12">
    <div class="dash__box dash__box--shadow dash__box--radius dash__box--bg-white u-s-m-b-30">
        <div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between p-b-20 border-bottom">
                    <div class="media align-items-center">
                        <div class="avatar avatar-blue avatar-icon" style="height: 55px; width: 55px;">
                            <i class="anticon anticon-gift" font-size-25" style="line-height: 55px"></i>
                        </div>
                        <div class="m-l-15">
                            <h3 class="font-weight-bold font-size-30 m-b-0">
                                Giảm 10%
                                @*                          @if (Model.LoaiHinhUuDai == 1)
                                {
                                <span>Giảm @Model.MucUuDai %</span>
                                }
                                else
                                {
                                double? mucUuDai = Model.MucUuDai;
                                @string.Format("{0:N0}đ", Model.MucUuDai)
                                // Định dạng giá trị theo định dạng tiền Việt Nam
                                } *@
                            </h3>
                            @*    @functions {
                            // Hàm trợ giúp để định dạng số nguyên thành tiền tệ Việt Nam
                            public string FormatCurrency(int? value)
                            {
                            return string.Format(new CultureInfo("vi-VN"), "{0:C0}", value);
                            }
                            public string FormatDate(DateTime? value)
                            {
                            if (value.HasValue)
                            {
                            return value.Value.ToString("dd.MM", new System.Globalization.CultureInfo("vi-VN"));
                            }
                            return string.Empty; // hoặc bạn có thể trả về một giá trị mặc định khác nếu cần
                            }

                            } *@
                            <h5 class="m-b-0">
                                <i class="anticon anticon-clock-circle"></i>
                                @*       Có hiệu lực từ @FormatDate(Model.NgayBatDau) *@
                                Có hiệu lực từ 14-02-03
                            </h5>
                        </div>
                    </div>
                </div>
                <ul class="list-unstyled m-v-30">
                    <li class="m-b-20">
                        <h4 class="m-b-0">  Giảm cho đơn từ 100k  @* @FormatCurrency(Model.DieuKien) *@</h4>
                        <div class="d-flex justify-content-between">
                            <p style="color:red">
                                <i class="anticon anticon-clock-circle"></i>
                                Hạn sử dụng mã từ @* @FormatDate(Model.NgayBatDau) *@ 14-02 - @* @FormatDate(Model.NgayKetThuc) *@ 20-10
                            </p>
                        </div>
                    </li>
                    <li class="m-b-20">
                    </li>
                    <li class="m-b-20">
                        <div class="d-flex justify-content-between">
                            <span class="text-dark font-weight-semibold">    Xem chi tiết</span>
                        </div>
                    </li>
                    <li class="m-b-20">
                        <div class="d-flex justify-content-between">
                            <p>
                                Xem chi tiết
                                Mã giảm theo tiền  @* @if (Model.LoaiHinhUuDai == 1)
                                {
                                // Chuyển đổi giá trị thành phần trăm và định dạng chuỗi
                                string mucUuDaiPhanTram = string.Format("{0:P}", Model.MucUuDai);
                                }
                                else
                                {
                                // Định dạng giá trị theo định dạng tiền Việt Nam
                                @string.Format("{0:N0}đ", Model.MucUuDai)

                                } *@ cho đơn hàng từ @* @FormatCurrency(Model.DieuKien) *@ 40K sử dụng trên trang web.<br />
                                Số lượng mã giảm giá còn hạn 10 @* @Model.SoLuong. *@ Hạn sử dụng đến @* @Model.NgayKetThuc *@ 20-10
                            </p>
                    </li>
                </ul>
                <div class="text-center">
                    <button class="btn btn-primary">Copy mã</button>
                </div>
            </div>
        </div>
    </div>
</div>
