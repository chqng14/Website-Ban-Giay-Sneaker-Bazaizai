@model IEnumerable<App_Data.ViewModels.GioHangChiTiet.GioHangChiTietDTO>

@{
    ViewData["Title"] = "CheckOutNoLogin";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@if (ViewData["QuantityErrorMessages"] != null)
{
    <script>
        var problemCountMessage = '@TempData["problemCount"]';
        var errorMessages = @Html.Raw(Json.Serialize(ViewData["QuantityErrorMessages"]));
        Swal.fire({
            title: "Sản phẩm lỗi",
            icon: 'error',
            html: errorMessages.join('<br/>'),
        })
    </script>
}
<link href="~/css/truck.css" rel="stylesheet" />
<div class="u-s-p-b-60" style="margin-top:20px">

    <!--====== Section Content ======-->
    <div class="section__content">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div id="checkout-msg-group">
                        @* <div class="msg u-s-m-b-30">

                        <span class="msg__text">
                        Returning customer?

                        <a class="gl-link" href="#return-customer" data-toggle="collapse">Click here to login</a>
                        </span>
                        <div class="collapse" id="return-customer" data-parent="#checkout-msg-group">
                        <div class="l-f u-s-m-b-16">

                        <span class="gl-text u-s-m-b-16">If you have an account with us, please log in.</span>
                        <form class="l-f__form">
                        <div class="gl-inline">
                        <div class="u-s-m-b-15">

                        <label class="gl-label" for="login-email">E-MAIL *</label>

                        <input class="input-text input-text--primary-style" type="text" id="login-email" placeholder="Enter E-mail">
                        </div>
                        <div class="u-s-m-b-15">

                        <label class="gl-label" for="login-password">PASSWORD *</label>

                        <input class="input-text input-text--primary-style" type="text" id="login-password" placeholder="Enter Password">
                        </div>
                        </div>
                        <div class="gl-inline">
                        <div class="u-s-m-b-15">

                        <button class="btn btn--e-transparent-brand-b-2" type="submit">LOGIN</button>
                        </div>
                        <div class="u-s-m-b-15">

                        <a class="gl-link" href="lost-password.html">Lost Your Password?</a>
                        </div>
                        </div>

                        <!--====== Check Box ======-->
                        <div class="check-box">

                        <input type="checkbox" id="remember-me">
                        <div class="check-box__state check-box__state--primary">

                        <label class="check-box__label" for="remember-me">Remember Me</label>
                        </div>
                        </div>
                        <!--====== End - Check Box ======-->
                        </form>
                        </div>
                        </div>
                        </div> *@


                        <div class="msg">
                            <span class="msg__text">
                                Bạn có Voucher?
                                <a class="gl-link" id="voucher-noLogin" @* data-modal="modal" data-modal-id="#voucher-modal" *@ @* href="#have-coupon" *@ data-toggle="collapse">Ấn vào đây để sử dụng Voucher</a>
                            </span>
                            @* <div class="collapse" id="have-coupon" data-parent="#checkout-msg-group">
                            <div class="c-f u-s-m-b-16">

                            <form class="c-f__form" asp-action="GetVoucherByMa" asp-controller="Voucher">
                            <div class="u-s-m-b-16">
                            <div class="u-s-m-b-15">

                            <label for="coupon"></label>

                            <input class="input-text input-text--primary-style" type="text" id="voucherinput" placeholder="Nhập phiếu giảm giá của bạn ở đây" name="voucher">
                            <label id="textvoucher" style="color:red"></label>
                            </div>
                            </div>
                            </form>
                            </div>
                            </div> *@
                            <div class="modal fade" id="voucher-modal">
                                <div class="modal-dialog modal-dialog-centered justify-content-center" style="min-width:550px">
                                    <div class="modal-content">
                                        <div class="modal-body">
                                            <div class="checkout-modal2">
                                                <div class="u-s-m-b-30">
                                                    <div class="dash-l-r">
                                                        <h1 class="gl-modal-h1">Chọn Voucher</h1>
                                                    </div>
                                                </div>
                                                <div class="c-f u-s-m-b-16">

                                                    <form class="c-f__form" asp-action="GetVoucherByMa" asp-controller="Voucher">
                                                        <div class="u-s-m-b-16">
                                                            <div class="u-s-m-b-15">

                                                                <label for="coupon"></label>

                                                                <input class="input-text input-text--primary-style" type="text" id="voucherinput" placeholder="Nhập phiếu giảm giá của bạn ở đây" name="voucher" style="width:200%">
                                                                <label id="textvoucher" style="color:red"></label>
                                                            </div>
                                                            <div class="u-s-m-b-15">

                                                                <button class="btn btn--e-transparent-brand-b-2" type="button" id="button-coupon">SỬ DỤNG</button>
                                                            </div>
                                                        </div>
                                                    </form>
                                                </div>
                                            </div>
                                        </div>
                                        <button class="btn dismiss-button fas fa-times" type="button" data-dismiss="modal"></button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!--====== End - Section Content ======-->
</div>
<!--====== End - Section 2 ======-->
<!--====== Section 3 ======-->
<div class="u-s-p-b-60">

    <!--====== Section Content ======-->
    <div class="section__content">
        <div class="container">
            <div class="checkout-f">
                <div class="row">
                    <div class="col-lg-6">
                        <h1 class="checkout-f__h1">THÔNG TIN GIAO HÀNG</h1>
                        <form class="checkout-f__delivery" id="deliveryForm" asp-action="DataBill" asp-controller="HoaDon">
                            <div class="u-s-m-b-30">
                                

                                <!--====== First Name, Last Name ======-->
                                <div class="gl-inline">
                                    <div class="u-s-m-b-15">

                                        <label class="gl-label" for="billing-fname">TÊN NGƯỜI NHẬN *</label>

                                        <input class="input-text input-text--primary-style" type="text" id="billing-fname" data-bill="" name="TenNguoiNhan">
                                    </div>
                                </div>
                                <!--====== End - First Name, Last Name ======-->
                                <!--====== E-MAIL ======-->
                                <div class="u-s-m-b-15">

                                    <label class="gl-label" for="billing-email">EMAIL *</label>

                                    <input class="input-text input-text--primary-style" type="email" id="billing-email" data-bill="" name="Email">
                                </div>
                                <!--====== End - E-MAIL ======-->
                                <!--====== PHONE ======-->
                                <div class="u-s-m-b-15">

                                    <label class="gl-label" for="billing-phone">SỐ ĐIỆN THOẠI *</label>

                                    <input class="input-text input-text--primary-style" type="text" id="billing-phone" data-bill="" name="SDT">
                                </div>
                                <!--====== End - PHONE ======-->
                                <!--====== Street Address ======-->
                                <div class="u-s-m-b-15">

                                    <label class="gl-label" for="billing-street">ĐỊA CHỈ *</label>

                                    <input class="input-text input-text--primary-style" type="text" id="billing-street" placeholder="Số nhà,số đường" data-bill="" name="DiaChi">
                                </div>


                                <div id="InputDropdown">
                                    <div class="u-s-m-b-15">

                                        <!--====== Select Box ======-->

                                        <label class="gl-label" for="city">THÀNH PHỐ *</label><select class="select-box select-box--primary-style" id="city" required>
                                            <option selected>Chọn thành phố</option>
                                        </select>
                                        <!--====== End - Select Box ======-->
                                    </div>
                                    <div class="u-s-m-b-15">

                                        <label class="gl-label" for="district">HUYỆN *</label><select class="select-box select-box--primary-style" id="district" required>
                                            <option selected>Chọn huyện</option>
                                        </select>
                                    </div>
                                    <div class="u-s-m-b-15">

                                        <!--====== Select Box ======-->

                                        <label class="gl-label" for="wards">PHƯỜNG/XÃ *</label><select class="select-box select-box--primary-style" id="wards" required>
                                            <option selected>Chọn phường/xã</option>
                                        </select>
                                        <!--====== End - Select Box ======-->
                                    </div>
                                </div>
                                @* <div class="u-s-m-b-10">

                                    <!--====== Check Box ======-->
                                    <div class="check-box">

                                        <input type="checkbox" id="make-default-address" data-bill="">
                                        <div class="check-box__state check-box__state--primary">

                                            <label class="check-box__label" for="make-default-address">Chọn làm địa chỉ mặc định</label>
                                        </div>
                                    </div>
                                    <!--====== End - Check Box ======-->
                                </div> *@
                                @* <div class="u-s-m-b-10">

                                    <a class="gl-link" href="#create-account" data-toggle="collapse">Tạo tài khoản mới?</a>
                                </div> *@
                                <div class="collapse u-s-m-b-15" id="create-account">

                                    <span class="gl-text u-s-m-b-15">Tạo tài khoản mới vui lòng nhập thông tin bên dưới. Nếu bạn đã có tài khoản vui lòng đăng nhập ở góc trên.</span>
                                    <div>

                                        <label class="gl-label" for="reg-password">Mật khẩu *</label>

                                        <input class="input-text input-text--primary-style" type="text" data-bill id="reg-password">
                                    </div>
                                </div>
                                <div class="u-s-m-b-10">

                                    <label class="gl-label" for="order-note">GHI CHÚ</label><textarea class="text-area text-area--primary-style" id="order-note"></textarea>
                                </div>
                                @* <div>

                                <button class="btn btn--e-transparent-brand-b-2" id="luu" type="submit">LƯU</button>
                                </div> *@
                            </div>
                        </form>
                    </div>
                    <div class="col-lg-6">
                        <h1 class="checkout-f__h1">SẢN PHẨM</h1>

                        <!--====== Order Summary ======-->
                        <div class="o-summary">
                            <form class="checkout-f__payment" id="paymentForm" asp-action="ThanhToan" asp-controller="HoaDon">
                                <div class="o-summary__section u-s-m-b-30">
                                    <div class="o-summary__item-wrap gl-scroll">
                                        @foreach (var item in Model)
                                        {
                                            <div class="o-card">
                                                <div class="o-card__flex">
                                                    <div class="o-card__img-wrap">

                                                        <img class="u-img-fluid" src="/AnhSanPham/@item.LinkAnh[0]" alt="">
                                                    </div>
                                                    <div class="o-card__info-wrap">

                                                        <span class="o-card__name">

                                                            <a href="@Url.Action("Details", "SanPhamChiTiets", new { id = item.IdSanPhamCT })">@item.TenThuongHieu @item.TenSanPham</a>
                                                        </span>
                                                        <span class="o-card__quantity">Size: @item.TenKichCo Màu: @item.TenMauSac</span>
                                                        <span class="o-card__quantity">Số lượng x @item.SoLuong</span>

                                                        <span class="o-card__price">@string.Format("{0:N0}đ", item.GiaBan)</span>
                                                    </div>
                                                </div>

                                                <a class="o-card__del far fa-trash-alt" href="@Url.Action("DeleteCartCheckOutNoLogin", "GioHangChiTiets", new { id = item.IdSanPhamCT })"></a>
                                            </div>
                                        }


                                    </div>
                                </div>
                                <div class="o-summary__section u-s-m-b-30">
                                    <div class="o-summary__box">
                                        <h1 class="checkout-f__h1">ĐỊA CHỈ GIAO HÀNG</h1>
                                        <div class="ship-b">

                                            <span class="ship-b__text">Giao tới:</span>
                                            <div class="ship-b__box u-s-m-b-10">
                                                <p class="ship-b__p" id="thongtin"></p>
                                                <a class="ship-b__edit btn--e-transparent-platinum-b-2" data-modal="modal" data-modal-id="#edit-ship-address">Sửa</a>
                                            </div>
                                            @* <div class="ship-b__box">

                                            <span class="ship-b__text">Bill to default billing address</span>

                                            <a class="ship-b__edit btn--e-transparent-platinum-b-2" data-modal="modal" data-modal-id="#edit-ship-address">Edit</a>
                                            </div> *@
                                        </div>
                                    </div>
                                </div>
                                <div class="o-summary__section u-s-m-b-30">
                                    <div class="o-summary__box">
                                        <table class="o-summary__table">
                                            <tbody>
                                                <tr>
                                                    <td>PHÍ SHIP</td>
                                                    <td id="phiship">0đ</td>
                                                    <input hidden name="TienShip" />
                                                </tr>
                                                <tr>
                                                    <td>TIỀN GIẢM</td>
                                                    <td id="TienGiam">0đ</td>
                                                    <input hidden name="TienGiam" />
                                                </tr>
                                                <tr>
                                                    <td>TẠM TÍNH</td>
                                                    <td id="tamtinh">@string.Format("{0:N0}đ", Model.Sum(x => x.SoLuong * x.GiaBan))</td>

                                                </tr>
                                                <tr>
                                                    <td>TỔNG TIỀN</td>
                                                    <td id="tongtien">@string.Format("{0:N0}đ", Model.Sum(x => x.SoLuong * x.GiaBan))</td>
                                                    <input hidden name="TongTien" />
                                                </tr>
                                            </tbody>
                                        </table>
                                        <input hidden name="MoTa" />
                                        <input hidden name="IdThongTinGH" />
                                    </div>
                                </div>
                                <div class="o-summary__section u-s-m-b-30">
                                    <div class="o-summary__box">
                                        <h1 class="checkout-f__h1">PHƯƠNG THỨC THANH TOÁN</h1>

                                        <div class="u-s-m-b-10">

                                            <!--====== Radio Box ======-->
                                            <div class="radio-box">

                                                <input type="radio" value="COD" id="cash-on-delivery" name="Payment">
                                                <div class="radio-box__state radio-box__state--primary">

                                                    <label class="radio-box__label" for="cash-on-delivery">COD</label>
                                                </div>
                                            </div>
                                            <!--====== End - Radio Box ======-->

                                            <span class="gl-text u-s-m-t-6">Thanh toán bằng tiền mặt khi giao hàng.</span>
                                        </div>
                                       @*  <div class="u-s-m-b-10">

                                            <!--====== Radio Box ======-->
                                            <div class="radio-box">

                                                <input type="radio" value="Momo" id="direct-bank-transfer" name="Payment">
                                                <div class="radio-box__state radio-box__state--primary">

                                                    <label class="radio-box__label" for="direct-bank-transfer">Momo</label>
                                                </div>
                                            </div>
                                            <!--====== End - Radio Box ======-->

                                            <span class="gl-text u-s-m-t-6">Khi bạn nhấp vào "Đặt hàng" bên dưới, chúng tôi sẽ chuyển hướng bạn đến trang web của Momo để thiết lập thông tin thanh toán của bạn.</span>
                                        </div> *@

                                        <div class="u-s-m-b-10">

                                            <!--====== Radio Box ======-->
                                            <div class="radio-box">

                                                <input type="radio" value="VnPay" id="direct-bank-transfer" name="Payment">
                                                <div class="radio-box__state radio-box__state--primary">

                                                    <label class="radio-box__label" for="direct-bank-transfer">VnPay</label>
                                                </div>
                                            </div>
                                            <!--====== End - Radio Box ======-->

                                            <span class="gl-text u-s-m-t-6">Khi bạn nhấp vào "Đặt hàng" bên dưới, chúng tôi sẽ chuyển hướng bạn đến trang web của VnPay để thiết lập thông tin thanh toán của bạn.</span>
                                        </div>
                                        <div class="u-s-m-b-15">

                                            <!--====== Check Box ======-->
                                            <div class="check-box">

                                                <input type="checkbox" id="term-and-condition" required>
                                                <div class="check-box__state check-box__state--primary">

                                                    <label class="check-box__label" for="term-and-condition">Tôi Đồng ý với</label>
                                                </div>
                                            </div>
                                            <!--====== End - Check Box ======-->

                                            <a class="gl-link">Điều khoản.</a>
                                        </div>
                                        <input hidden name="LoaiThanhToan" />
                                        <input hidden name="IdVoucher" />
                                        <input hidden name="NgayGiaoDuKien" />
                                        <div>
                                            <button id="placeOrderButton" class="btn btn--e-brand-b-2" type="submit">ĐẶT HÀNG</button>
                                        </div>

                                    </div>
                                </div>
                            </form>
                        </div>
                        <!--====== End - Order Summary ======-->
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!--====== End - Section Content ======-->
</div>
<script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.1/moment.min.js"></script>
<script src="https://kjur.github.io/jsrsasign/jsrsasign-latest-all-min.js"></script>
<script src="https://crypto-js.googlecode.com/svn/tags/3.1.2/build/rollups/md5.js"></script>
@section Scripts{

    <!--====== Lấy ra địa chỉ ======-->
    <script>
        var isCalculatingShippingFee = false;
        var tongtienSanPham = parseInt(document.getElementById("tamtinh").innerText.replace("đ", "").replace(/\,/g, "").replace(/\./g, "").trim());
        var totalAmountElement = document.getElementById("tongtien");
        var MucUuDai = "";
        var phiship = 0;
        var loaiuudai = "";
        var tongTienSauGiamGia = 0;
        let hasFunctionRun = false;
        $(document).ready(function () {
         $('#phiship').text('0đ');
         phiship = 0;
                if (MucUuDai !== null && MucUuDai !== "") {
                    totalAmountElement.innerText = tongTienSauGiamGia.toLocaleString("vi-VN").replace(/\./g, ',') + "đ";
                    totalAmountElement.value = tongTienSauGiamGia.toString();
                } else {
                    totalAmountElement.innerText = tongtienSanPham.toLocaleString("vi-VN").replace(/\./g, ',') + "đ";
                    totalAmountElement.value = tongtienSanPham.toString();
                }
                useInputAddressScript();
                

            function useInputAddressScript() {
                $(document).ready(function () {
                    const apiUrl = 'https://online-gateway.ghn.vn/shiip/public-api/master-data/province';
                    const token = '1194852d-fde8-11ed-8a8c-6e4795e6d902';

                    axios.get(apiUrl, {
                        headers: {
                            'Token': token
                        }
                    })
                        .then(response => {
                            var selectElement = $('#city');
                            selectElement.empty();

                            selectElement.append($('<option>').val('').text('Chọn thành phố'));

                            $.each(response.data.data, function (index, item) {
                                selectElement.append($('<option>').val(item.ProvinceID).text(item.ProvinceName));
                            });

                            selectElement.val(selectElement.val()).trigger('change');
                        })
                        .catch(error => {
                            console.error(error);
                        });
                });

                $(document).ready(function () {
                    $('#city').on('change', function () {
                        if ($('#city').val() === null) {
                            var selectElement = $('#district');
                            selectElement.empty();
                            selectElement.val('').trigger('change');
                            selectElement.append($('<option>').val('').text('Chọn huyện'));
                        }
                        const apiUrl = 'https://online-gateway.ghn.vn/shiip/public-api/master-data/district';
                        const token = '1194852d-fde8-11ed-8a8c-6e4795e6d902';
                        axios.get(apiUrl, {
                            headers: {
                                'Token': token
                            },
                            params: {
                                province_id: $('#city').val()
                            }
                        })
                            .then(response => {
                                var selectElement = $('#district');
                                selectElement.empty();
                                selectElement.val('').trigger('change');
                                selectElement.append($('<option>').val('').text('Chọn huyện'));
                                $.each(response.data.data, function (index, item) {
                                    selectElement.append($('<option>').val(item.DistrictID).text(item.DistrictName));
                                });
                                selectElement.val(selectElement.val()).trigger('change');
                            })
                            .catch(error => {
                                console.error(error);
                            });
                    });
                });

                $(document).ready(function () {
                    $('#district').on('change', function () {
                        if ($('#district').val() === null) {
                            var selectElement = $('#wards');
                            selectElement.empty();
                            selectElement.val('').trigger('change');
                            selectElement.append($('<option>').val('').text('Chọn phường/xã'));
                        }
                        const apiUrl = 'https://online-gateway.ghn.vn/shiip/public-api/master-data/ward';
                        const token = '1194852d-fde8-11ed-8a8c-6e4795e6d902';
                        axios.get(apiUrl, {
                            headers: {
                                'Token': token
                            },
                            params: {
                                district_id: $('#district').val()
                            }
                        })
                            .then(response => {
                                var selectElement = $('#wards');
                                selectElement.empty();
                                selectElement.val('').trigger('change');
                                selectElement.append($('<option>').val('').text('Chọn phường/xã'));
                                $.each(response.data.data, function (index, item) {
                                    selectElement.append($('<option>').val(item.WardCode).text(item.WardName));
                                });
                                selectElement.val(selectElement.val()).trigger('change');
                            })
                            .catch(error => {
                                console.error(error);
                            });
                    });
                });

                $(document).ready(function () {
                    $('#wards').on('change', function () {
                        if ($('#wards').val() != null) {
                            isCalculatingShippingFee = true;
                            calculateShippingFee();
                            isCalculatingShippingFee = false;
                        }
                    });
                });
            }
        });

    </script>

    <!--====== Tính phí ship và voucher ======-->
    <script>
        //khi ô input đc thay đổi
        // $('#voucherinput').on('change', function () {
        //     MucUuDai = "";
        //     voucher(this.value);
        // });
        //khi ấn enter
        $('#voucherinput').on('keydown', function (e) {
            if (e.key === 'Enter') {
                e.preventDefault();
                MucUuDai = "";
                voucher(this.value);
            }
        });
        // Khi chọn ship sau
        // $('#get-address').on('change', function () {
        //     var useDefaultAddress = $(this).prop('checked');

        //         totalAmountElement.innerText = tongtienSanPham.toLocaleString("vi-VN").replace(/\./g, ',') + "đ";
        //         totalAmountElement.value = tongtienSanPham.toString();


        //     // calculateShippingFee();
        // });
        // Hàm tính phí ship
        var serviceid = null
        function availableservices() {
            var ApiUrl = 'https://online-gateway.ghn.vn/shiip/public-api/v2/shipping-order/available-services';
            var token = '1194852d-fde8-11ed-8a8c-6e4795e6d902';
            var shop_id = 705899;
            return new Promise((resolve, reject) => {
                axios.get(ApiUrl, {
                    headers: {
                        'Token': token,
                    },
                    params: {
                        from_district: 3440,
                        to_district: $('#district').val(),
                        shop_id: shop_id
                    }
                })
                    .then(response => {
                        response.data.data.forEach(service => {
                            if (service.service_type_id === 2) {
                                serviceid = service.service_id;
                                resolve(); // Báo hiệu rằng availableservices đã hoàn thành
                            }
                        });
                    })
                    .catch(error => {
                        console.error(error);
                        reject(error); // Báo hiệu rằng có lỗi xảy ra
                    });
            });
        }

        function calculateDayShip() {
            availableservices().then(() => {
                var dayshipApiUrl = 'https://online-gateway.ghn.vn/shiip/public-api/v2/shipping-order/leadtime';
                var token = '1194852d-fde8-11ed-8a8c-6e4795e6d902';
                var shop_id = 705899;

                axios.get(dayshipApiUrl, {
                    headers: {
                        'Token': token,
                        'shop_id': shop_id
                    },
                    params: {
                        service_id: serviceid,
                        to_ward_code: $('#wards').val(),
                        to_district_id: $('#district').val(),
                    }
                }).then(response => {
                    // Gán tiền ship vào id phiship
                    var date = moment.unix(response.data.data.leadtime).format("YYYY-MM-DD HH:mm:ss");
                    document.querySelector('input[name="NgayGiaoDuKien"]').value = date;
                })
                    .catch(error => {
                        console.error(error);
                    });
            }).catch(error => {
                console.error(error);
            });
        }

        function calculateShippingFee() {
            var inputs = document.getElementsByClassName('input-counter__text');
            var sum = 0;
            for (var i = 0; i < inputs.length - 1; i++) {
                var inputValue = parseInt(inputs[i].value);
                if (!isNaN(inputValue)) {
                    sum += inputValue;
                }
            }

            var soLuongDonHang = sum;
            var length = 20; // Giá trị mặc định cho chiều dài
            var width = 15; // Giá trị mặc định cho chiều rộng
            var height = 20;
            var weight = parseInt((length * width * height) / 5000);
            var shippingApiUrl = 'https://online-gateway.ghn.vn/shiip/public-api/v2/shipping-order/fee';
            var token = '1194852d-fde8-11ed-8a8c-6e4795e6d902';
            var shop_id = 705899;

            axios.get(shippingApiUrl, {
                headers: {
                    'Token': token,
                    'shop_id': shop_id
                },
                params: {
                    service_type_id: 2,
                    insurance_value: $('#tamtinh').text().replace(/\D/g, ''),
                    coupon: null,
                    to_ward_code: $('#wards').val(),
                    to_district_id: $('#district').val(),
                    from_district_id: 3440,
                    weight: weight,
                    length: length,
                    width: width,
                    height: height
                }
            })
                .then(response => {
                    // Gán tiền ship vào id phiship
                    phiship = response.data.data.service_fee;
                    console.log(phiship);
                    $('#phiship').text(parseInt(phiship).toLocaleString("vi-VN").replace(/\./g, ',') + "đ");

                    // Lấy ra tổng tiền của sản phẩm có trong giỏ hàng
                    if (MucUuDai !== null && MucUuDai !== "") {
                        var tongTien = tongtienSanPham - MucUuDai + phiship;
                        document.querySelector('input[name="TongTien"]').value = tongtienSanPham;
                    }
                    else {
                        var tongTien = tongtienSanPham + phiship;
                        document.querySelector('input[name="TongTien"]').value = tongtienSanPham;
                    }
                    // Hiển thị tổng tiền
                    totalAmountElement.innerText = tongTien.toLocaleString("vi-VN").replace(/\./g, ',') + "đ";
                    totalAmountElement.value = tongTien.toString();
                    // Gán giá trị vào input
                    document.querySelector('input[name="TienShip"]').value = phiship;
                    calculateDayShip();
                    isCalculatingShippingFee = false;
                })
                .catch(error => {
                    console.error(error);
                });
        }

        // Hàm áp dụng voucher
        // function applyVoucher() {
        //     var voucherValue = document.getElementById("voucherinput").value;
        //     voucher(voucherValue);
        // }

        // Hàm xử lý voucher
        $(document).ready(function () {
            $('#voucher-noLogin').click(function () {
                toast({
                    title: "Vui lòng đăng nhập để sử dụng voucher!",
                    type: "warning",
                    duration: 2000
                });
            });

        });

        var IdVoucher = "";
        function voucher(value) {
            if (value != null && value.trim() != "") {
                var ma = { ma: value };
                $.ajax({
                    url: '/Voucher/GetVoucherByMa',
                    type: 'GET',
                    data: ma,
                    success: function (result) {
                        $('#textvoucher').text("");
                        loaiuudai = result.loaiuudai;
                        // Áp dụng giảm giá từ voucher
                        var storedVouchers = JSON.parse(localStorage.getItem("Vouchers")) || {}
                        if (storedVouchers[result.idVoucher]) {
                            toast({
                                title: "Bạn đã sử dụng voucher này rồi,vui lòng sử dụng voucher khác!",
                                type: "warning",
                                duration: 2000
                            });
                        }else if (phiship == 0 && loaiuudai == 2) {
                            Swal.fire({
                                title: "Vui lòng nhập địa chỉ trước khi áp dụng freeship",
                                icon: "warning",
                                showConfirmButton: false,
                                timer: 1500
                            });
                        } else {
                            toast({
                                title: "Áp dụng thành công!",
                                type: "success",
                                duration: 2000
                            });
                            MucUuDai = result.mucuidai;
                            IdVoucher = result.idVoucher;
                            $('input[name="IdVoucher"]').val(IdVoucher);
                            applyDiscount(MucUuDai);
                        }
                    },
                    error: function (xhr, status, error) {
                        MucUuDai = "";
                        $('#textvoucher').text("mã không hợp lệ");
                        applyDiscount(0);
                    }
                });
            } else {
                $('#textvoucher').text("");
                // Nếu không có voucher, hiển thị tổng tiền không giảm giá
                applyDiscount(0);
            }
        }

        // Hàm áp dụng giảm giá
        function applyDiscount(discountAmount) {
            if (discountAmount == null || discountAmount == 0) {
                $('#TienGiam').text(discountAmount.toLocaleString("vi-VN") + "đ");
            }
            else if (loaiuudai == 0) {
                $('#TienGiam').text("-" + discountAmount.toLocaleString("vi-VN") + "đ");
            }
            else if (loaiuudai == 2) {
                discountAmount = phiship;
                $('#TienGiam').text("-" + discountAmount.toLocaleString("vi-VN") + "đ");
            }
            else {
                discountAmount = tongtienSanPham * discountAmount / 100;
                $('#TienGiam').text("-" + discountAmount.toLocaleString("vi-VN") + "đ");
            }
            document.querySelector('input[name="TienGiam"]').value = discountAmount;
            // Tính tổng tiền sau khi giảm giá
            var tamTinhGiaGiam = tongtienSanPham + parseFloat(phiship);
            tongTienSauGiamGia = tamTinhGiaGiam - discountAmount;
            // Hiển thị tổng tiền sau khi giảm giá
            totalAmountElement.innerText = tongTienSauGiamGia.toLocaleString("vi-VN").replace(/\./g, ',') + "đ";
            totalAmountElement.value = tongTienSauGiamGia.toString();
        }
        function UpdateVoucher() {
            if (MucUuDai != null) {
                $.ajax({
                    url: '/Voucher/UpdateVouchersoluong',
                    type: 'POST',
                    data: { idVoucher: IdVoucher },
                    success: function (result) {
                        console.log("UpdateVouchersoluong");
                    },
                    error: function (xhr, status, error) {
                        console.log(error);
                    }
                });
            } else {
                $('#textvoucher').text("");
                // Nếu không có voucher, hiển thị tổng tiền không giảm giá
                applyDiscount(0);
            }
        }

        function createAndStoreJwt(voucherCode) {
            // Tạo JWT khi cấp voucher
            var secretKey = "5a25e33fd1c5b4a882d438cf93dd4ac5eef0a27b8b3c7f7d6b5a84d4fb91e4f2";

            // Thời gian hết hiệu lực của JWT (ví dụ: 5 năm)
            var expirationTime = Math.floor(Date.now() / 1000) + 60 * 60 * 24 * 365 * 5;
            var storedVouchers = JSON.parse(localStorage.getItem("Vouchers")) || {};
            // Đặt giá trị cho Issuer và Audience
            var issuer = "BazaizaiStore"; // Người phát hành
            var audience = "User"; // Đối tượng

            // Tạo payload của JWT
            var payload = {
                iss: issuer,
                aud: audience,
                exp: expirationTime, // Thời gian hết hiệu lực
                voucherCode: voucherCode // Thông tin voucher
            };

            // Tạo JWT bằng cách ký payload với khóa bí mật
            var jwtToken = KJUR.jws.JWS.sign(null, { alg: "HS256", cty: "JWT" }, payload, secretKey);

            // Lưu JWT vào danh sách các voucher trong localStorage
            storedVouchers[voucherCode] = true;
            localStorage.setItem("Vouchers", JSON.stringify(storedVouchers));

            // Thực hiện các hành động khác nếu cần
        }
    </script>

    <!--====== thanh toán ======-->
    <script>
        // Lấy tất cả các elements input có type là radio
        var radioButtons = document.querySelectorAll('input[name="Payment"]');
        var paymentMethod = "";
        // Duyệt qua từng radio button
        radioButtons.forEach(function (radioButton) {
            // Lắng nghe sự kiện khi có thay đổi trạng thái của radio button
            radioButton.addEventListener('change', function () {
                // Nếu radio button được chọn
                if (this.checked) {
                    // Lấy giá trị của radio button và gán vào input hidden
                    paymentMethod = this.value;
                    // document.querySelector('input[name="LoaiThanhToan"]').value = this.value;
                    console.log(this.value);
                }
            });
        });
        function kiemTraTuyChon() {
            var cityInput = document.getElementById("city");
            var districtInput = document.getElementById("district");
            var wardsInput = document.getElementById("wards");
            var billingFnameInput = document.getElementById("billing-fname");
            var billingEmailInput = document.getElementById("billing-email");
            var billingPhoneInput = document.getElementById("billing-phone");
            var billingStreetInput = document.getElementById("billing-street");

            var isValid = true;
            isValid = isValid && cityInput.value !== "";
            isValid = isValid && districtInput.value !== "";
            isValid = isValid && wardsInput.value !== "";
            isValid = isValid && billingFnameInput.value !== "";
            isValid = isValid && billingEmailInput.value !== "";
            isValid = isValid && billingPhoneInput.value !== "";
            isValid = isValid && billingStreetInput.value !== "";

            if (!isValid) {
                toast({
                    title: "Vui lòng nhập đầy đủ thông tin!",
                    type: "warning",
                    duration: 2000
                });
            }
            return !isValid;
        }
        function showLoadingOverlay() {
            $('#placeOrderButton').html('<span class="loader"></span>');
        }

        function hideLoadingOverlay() {
            $('.loader').remove();
        }
        var agreeCheckbox = document.getElementById("term-and-condition");
        $(document).ready(function () {
            $('#placeOrderButton').click(function (e) {
                e.preventDefault();
                if (kiemTraTuyChon()) {
                    hideLoadingOverlay();
                    return;
                }
                if (!paymentMethod) {
                    hideLoadingOverlay();
                    toast({
                        title: "Vui lòng chọn phương thức thanh toán!",
                        type: "warning",
                        duration: 2000
                    });
                    return;
                }
                if (!agreeCheckbox.checked) {
                    hideLoadingOverlay();
                    toast({
                        title: "Vui lòng đồng ý với điều khoản!",
                        type: "warning",
                        duration: 2000
                    });
                    return;
                }
                var selectedCity, selectedDistrict, selectedWard, inputAddressBill, DiaChiFull;


                // Sử dụng useInputAddressScript
                selectedCity = $('#city option:selected').text();
                selectedDistrict = $('#district option:selected').text();
                selectedWard = $('#wards option:selected').text();

                inputAddressBill = $('#billing-street').val();
                DiaChiFull = inputAddressBill + ',' + selectedWard + ',' + selectedDistrict + ',' + selectedCity;
                $('#billing-street').val(DiaChiFull);
                $('input[name="LoaiThanhToan"]').val(paymentMethod);
                // Thực hiện các chức năng liên quan đến useInputAddressScript
                var deliveryFormData = $('#deliveryForm').serialize();
                showLoadingOverlay();
                // Gửi dữ liệu DataBill
                $.ajax({
                    type: 'POST',
                    url: '/HoaDon/DataBillNologin',
                    data: deliveryFormData,
                    success: function (deliveryResponse) {
                        // Thực hiện Ajax để gửi dữ liệu đến action ThanhToan trong controller TenControllerKhac
                        if (deliveryResponse.idThongTinGH != null) {
                            var moTaValue = $("#order-note").val();
                            $('input[name="MoTa"]').val(moTaValue);
                            $("[name='IdThongTinGH']").val(deliveryResponse.idThongTinGH)

                            var paymentFormData = $('#paymentForm').serialize();
                            // Gửi dữ liệu thanh toán
                            $.ajax({
                                type: 'POST',
                                url: '/HoaDon/ThanhToanNologin',
                                data: paymentFormData,
                                success: function (paymentResponse) {
                                    if (paymentResponse.idHoaDon != null) {
                                        if (IdVoucher != "") {
                                            createAndStoreJwt(IdVoucher);
                                            UpdateVoucher()
                                        }
                                        if (paymentResponse.url != null) {
                                            window.location.href = paymentResponse.url;
                                        } else {
                                            window.location.href = "/HoaDon/Order?idHoaDon=" + paymentResponse.idHoaDon;
                                        }
                                    } else {
                                        hideLoadingOverlay();
                                        Swal.fire({
                                            title: "Sản phẩm lỗi",
                                            icon: 'error',
                                            html: paymentResponse.message.join('<br/>'),
                                        })
                                    }
                                },
                                error: function (paymentError) {
                                    hideLoadingOverlay();
                                    // Xử lý lỗi nếu cần
                                }
                            });
                        } else {
                            hideLoadingOverlay();
                            $('#billing-street').val(inputAddressBill);
                            Swal.fire({
                                title: "Sản phẩm lỗi",
                                icon: 'error',
                                html: deliveryResponse.message.join('<br/>'),
                            })
                        }
                    },
                    error: function (deliveryError) {
                        hideLoadingOverlay();
                        // Xử lý lỗi nếu cần
                    }
                });

            });
        });
    </script>

}